# GetBuiltFast — required env vars
# Copy to .env.local for development; set the same vars in Vercel for production.

# Transactional email (used by /api/contact and /api/submit-brief)
RESEND_API_KEY=

# Supabase (project: cbzqhcbstgeqizgvkmfd)
# Server-side only. Do NOT expose the service role key to the client.
GBF_SUPABASE_URL=https://cbzqhcbstgeqizgvkmfd.supabase.co
GBF_SUPABASE_SERVICE_ROLE=

# Stripe — Fix 6
# Start with test keys. Flip to live only after test-mode validation + Zal approval.
GBF_STRIPE_SECRET_KEY_TEST=
NEXT_PUBLIC_GBF_STRIPE_PUBLISHABLE_KEY_TEST=
GBF_STRIPE_WEBHOOK_SECRET_TEST=
